export default class Api {
    testApi() {
    }     //just a test api
    login() {
    }     //just a test api
    getAllLands() {
    }     //just a test api
    addLand() {
    }     //just a test api
    getCurrentOwnerDetails() {
    }     //just a test api
    getPreviousOwners() {
    }     //just a test api
    getTransactionDataList() {
    }     //just a test api
    getAllUsers() {
    }     //just a test api
    changeUserStatus() {
    }     //just a test api
    getPdfLink() {
    }     //just a test api
    sendPapersToBoth() {
    }     //just a test api
    updateVerificationList() {
    }     //just a test api
    makePdf() {
    }     //just a test api
    haltTransaction() {
    }     //just a test api
}
